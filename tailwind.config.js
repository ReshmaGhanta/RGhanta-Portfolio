// tailwind.config.js
/** @type {import('tailwindcss').Config} */

module.exports = {
    theme: {
      extend: {
        fontFamily: {
          montserrat: ['Montserrat', 'sans-serif'], // for headings
          opensans: ['Open Sans', 'sans-serif'],     // for body
        },
      },
    },
    plugins: [],
  }
  